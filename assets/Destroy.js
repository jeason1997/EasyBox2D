cc.Class({
    extends: cc.Component,
    
    editor: {
        executeInEditMode: true,
    },

    onLoad: function () {
        if (CC_EDITOR)
        {
         var fs = require('fs');
           
        }
    },
    
});
